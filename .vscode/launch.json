{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Markdown Reader",
      "type": "extensionHost",
      "request": "launch",
      "outFiles": ["${workspaceFolder}/dist/**/*.js"],
      "preLaunchTask": "npm: build"
    }
  ]
}
